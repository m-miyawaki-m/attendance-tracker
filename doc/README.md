# 勤怠管理システム（TypeScript版）

Vue 3 + TypeScript + Vuetify 3 + Vitest を使用した勤怠管理システムのモックアプリケーション

## 技術スタック

- **フロントエンド**: Vue 3 (Composition API)
- **言語**: TypeScript
- **UIフレームワーク**: Vuetify 3
- **状態管理**: Pinia
- **ルーティング**: Vue Router
- **グラフ**: ApexCharts
- **テスト**: Vitest + Vue Test Utils
- **ビルドツール**: Vite
- **コード品質**: ESLint + Prettier

## 必須要件

- Node.js v18以上
- npm または yarn

## セットアップ

### 1. プロジェクト作成

```bash
npm create vue@latest attendance-tracker

# TypeScript, Router, Pinia, Vitest, ESLint, Prettier を有効化
```

### 2. 依存関係インストール

```bash
cd attendance-tracker
npm install

# Vuetify & ApexCharts
npm install vuetify@next @mdi/font apexcharts vue3-apexcharts

# TypeScript型定義
npm install --save-dev @types/node

# テストライブラリ
npm install --save-dev @vue/test-utils @vitest/ui jsdom
```

### 3. プロジェクト起動

```bash
# 開発サーバー起動
npm run dev

# ブラウザで http://localhost:5173 を開く
```

## 利用可能なコマンド

```bash
# 開発サーバー起動
npm run dev

# TypeScript型チェック
npm run type-check

# プロダクションビルド
npm run build

# ビルド結果プレビュー
npm run preview

# ユニットテスト実行
npm run test:unit

# テストをウォッチモードで実行
npm run test:unit -- --watch

# カバレッジ付きでテスト実行
npm run test:coverage

# テストUI表示
npm run test:unit:ui

# Lint実行
npm run lint

# コードフォーマット
npm run format
```

## ディレクトリ構造

```
attendance-tracker/
├── src/
│   ├── assets/           # 静的アセット
│   ├── components/       # 共通コンポーネント
│   ├── data/            # モックデータ
│   │   └── mockData.ts
│   ├── plugins/         # プラグイン設定
│   │   └── vuetify.ts
│   ├── router/          # ルーター設定
│   │   └── index.ts
│   ├── stores/          # Piniaストア
│   │   └── auth.ts
│   ├── types/           # TypeScript型定義
│   │   └── index.ts
│   ├── views/           # ページコンポーネント
│   │   ├── LoginView.vue
│   │   ├── HomeView.vue
│   │   ├── AttendanceListView.vue
│   │   └── admin/
│   │       ├── DashboardView.vue
│   │       ├── EmployeeListView.vue
│   │       ├── AttendanceEditView.vue
│   │       └── TeamView.vue
│   ├── App.vue
│   └── main.ts
├── tests/
│   └── unit/
│       ├── data/
│       │   └── mockData.spec.ts
│       ├── stores/
│       │   └── auth.spec.ts
│       └── views/
│           └── LoginView.spec.ts
├── package.json
├── tsconfig.json
├── vite.config.ts
└── vitest.config.ts
```

## 機能一覧

### 実装済み機能

- ✅ ログイン/ログアウト（モック認証）
- ✅ 出退勤打刻（位置情報取得）
- ✅ 月次勤怠一覧表示
- ✅ 管理ダッシュボード（グラフ表示）
- ✅ 従業員一覧表示
- ✅ 勤怠情報編集（管理者のみ）
- ✅ 担当社員確認
- ✅ レスポンシブデザイン
- ✅ TypeScript型定義
- ✅ ユニットテスト

### 未実装（拡張予定）

- ✅ Firebase統合（Auth + Firestore）
- ✅ 実際の認証機能
- 🔲 リアルタイムデータ同期
- 🔲 E2Eテスト
- 🔲 PWA化

## 開発環境

詳細は [開発フロー](./DEVELOPMENT_FLOW.md) を参照してください。

### ブランチ戦略

| ブランチ | 環境 | Firebase |
|---------|------|----------|
| `main` | 本番 (Vercel Production) | 本番Firebase |
| `develop` | ステージング (Vercel Preview) | 本番Firebase |
| `feature/*` | ローカルのみ | Firebase Emulator |

### ローカル開発の始め方

```bash
# 1. Emulator起動
npm run firebase:emulators

# 2. テストデータをシード
npm run seed:emulator

# 3. 開発サーバー起動
npm run dev
```

詳細は [Firebase Emulator セットアップ](./FIREBASE_EMULATOR_SETUP.md) を参照してください。

## テストアカウント

### ローカル開発（Firebase Emulator）

| メールアドレス | パスワード | 役割 |
|---------------|-----------|------|
| admin@example.com | adminadmin | 管理者 |
| user01@example.com | user01 | 一般ユーザー |
| user02~20@example.com | password123 | 一般ユーザー |

### 本番/ステージング

本番Firebaseに登録されているアカウントを使用してください。

## 画面一覧

1. **ログイン画面** (`/login`)
2. **ホーム画面（打刻）** (`/`)
3. **月次勤怠一覧** (`/attendance`)
4. **管理ダッシュボード** (`/admin/dashboard`) - 管理者のみ
5. **従業員一覧** (`/admin/employees`) - 管理者のみ
6. **勤怠編集** (`/admin/edit/:id`) - 管理者のみ
7. **担当社員** (`/admin/team`) - 管理者のみ

## テスト実行例

```bash
# 全てのテストを実行
npm run test:unit

# 特定のファイルのみテスト
npm run test:unit LoginView.spec.ts

# ウォッチモードで実行
npm run test:unit -- --watch

# カバレッジレポート生成
npm run test:coverage
```

## 型チェック

```bash
# TypeScript型チェック実行
npm run type-check
```

## ビルド

```bash
# プロダクションビルド
npm run build

# ビルド結果は dist/ に出力される
```

## トラブルシューティング

### TypeScriptエラーが表示される

```bash
npm run type-check
```

### テストが失敗する

```bash
# node_modulesを削除して再インストール
rm -rf node_modules package-lock.json
npm install
```

### Vuetifyのコンポーネントが表示されない

`src/plugins/vuetify.ts` が正しくインポートされているか確認

## 開発ガイドライン

### コーディング規約

- TypeScriptの型定義を必ず使用
- Composition APIを使用
- ESLintとPrettierの設定に従う
- コンポーネントは単一責任の原則に従う

### テストガイドライン

- 新しい機能には必ずテストを追加
- カバレッジは80%以上を目標
- モックは適切に使用

### コミット規約

```
feat: 新機能
fix: バグ修正
docs: ドキュメント更新
style: コードスタイル修正
refactor: リファクタリング
test: テスト追加・修正
chore: その他の変更
```

## ライセンス

MIT

## 作成者

勉強会提出用プロジェクト

## 参考資料

- [Vue 3公式ドキュメント](https://vuejs.org/)
- [Vuetify 3公式ドキュメント](https://vuetifyjs.com/)
- [Vitest公式ドキュメント](https://vitest.dev/)
- [TypeScript公式ドキュメント](https://www.typescriptlang.org/)
- [Pinia公式ドキュメント](https://pinia.vuejs.org/)
